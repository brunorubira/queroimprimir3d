# Banco de dados (PostgreSQL)
# Em produção, use a string de conexão do Neon ou Supabase
DATABASE_URL="postgresql://user:password@localhost:5433/queroimprimir3d?schema=public"

# Porta do servidor NestJS
PORT=3001

# Segredo para assinar os tokens JWT
# IMPORTANTE: Use um segredo forte e único em produção!
JWT_SECRET="troque-por-uma-chave-muito-secreta-aqui"
